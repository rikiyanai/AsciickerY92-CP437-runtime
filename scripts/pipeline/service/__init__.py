"""Asset generation service layer.

Provides AssetService as the single entry point for all asset pipeline
operations, plus configuration types (AssetJobConfig, SlicingSpec,
BackgroundSpec) and resolver utilities.
"""
