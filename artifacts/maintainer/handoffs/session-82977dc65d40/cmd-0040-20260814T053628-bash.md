# Command Handoff

- Session key: `82977dc65d40`
- Sequence: `0040`
- Timestamp (UTC): `2026-08-14T05:36:28.501986+00:00`
- Tool: `Bash`

## Details

```json
{
  "command": "git show :assets/actor_visual_profiles/current/actor_visual_profile_provenance.json | python3 -c \"\nimport json,sys\nd=json.load(sys.stdin)\nprint(list(d.keys())[:20])\nfor k in ('row_count','entry_count'):\n    if k in d: print(k,d[k])\nrows=d.get('rows') or d.get('entries')\nif rows: \n    print('rows',len(rows))\n    w=[r for r in rows if 'wallace' in json.dumps(r)]\n    g=[r for r in rows if 'gromit' in json.dumps(r)]\n    print('wallace rows',len(w),'gromit rows',len(g))\n    if w: print(json.dumps(w[0])[:600])\n\"; echo; git diff --cached -- docs/research/ascii/semantic_maps/upstream_xp_cell_contract/compiler_cutover.json scripts/dump_actor_visual_reachability.py",
  "preview": "git show :assets/actor_visual_profiles/current/actor_visual_profile_provenance.json | python3 -c \" import json,sys d=json.load(sys.stdin) print(list(d.keys())[:20]) for k in ('row_count','entry_count'): if k in d: print(k,d[k]) rows=d.get('rows') or d.get('entries') if rows: print('rows',len(rows..."
}
```
