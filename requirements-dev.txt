# Dev + test dependencies — needed for local development, testing, and CI tooling.
# Install: pip install -r requirements-dev.txt
# (inherits all runtime deps)

-r requirements.txt

# Testing
pytest>=7.0.0
playwright>=1.40.0

# MCP dev server
mcp>=1.0.0

# OPTION A CodeRefs file:line -> function resolver
tree-sitter
tree-sitter-cpp

# Helper bot (launcher_helper_bot) — RAG over transcripts + code
tree-sitter-python
chromadb>=1.5.0
