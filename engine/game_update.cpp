// game_update.cpp — Main per-frame update path
//
// Extracted from engine/game.cpp.
// Owns the main per-frame update tick (GameUpdate, GameFrame, etc.).
//
// SEE ALSO:
// - engine/game.cpp — orchestrator
// - engine/game_input.cpp — input processing

#include "game.h"

// (stub) — main per-frame update path still in game.cpp.
// Extraction requires splitting the UpdateGame/GameFrame call chain.
